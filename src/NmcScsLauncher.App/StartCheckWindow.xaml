<Window x:Class="NmcScsLauncher.App.StartCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Startprüfung"
        Width="720"
        Height="610"
        MinWidth="640"
        MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource PrimaryTextBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="HeadingTextBlock" FontSize="25" FontWeight="Bold" />
            <TextBlock Text="NMC prüft Installation, Home-Verzeichnis und Startparameter, bevor das Spiel gestartet wird."
                       Margin="0,7,0,0" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="1" Background="#151E29" CornerRadius="8" Padding="12" Margin="0,18,0,16">
            <StackPanel>
                <TextBlock Text="Home-Basis" Foreground="{StaticResource SecondaryTextBrush}" />
                <TextBlock x:Name="HomePathTextBlock" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Checks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#111821" BorderBrush="#263342" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="82" />
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Severity}" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="2" Text="{Binding Message}" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ResultTextBlock" VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" />
            <Button Grid.Column="1" Content="Abbrechen" Padding="16,8" Margin="12,0,8,0" Click="Cancel_Click" />
            <Button Grid.Column="2" x:Name="StartButton" Content="Spiel starten" Padding="18,8" Background="#238636" Foreground="White" Click="Start_Click" />
        </Grid>
    </Grid>
</Window>
